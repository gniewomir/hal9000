# Vault (this repo)

- **YAML front matter (`---` … `---`):** Never modify the `id:` field—no edits, renames, reformatting, or new UUIDs. Existing `id` is read-only.
- **Do not** add `id:` or generate UUIDs yourself; vault scripts assign first-time ids.
- **Markdown links:** When creating or updating Markdown here, use `[text](url)` only—not HTML `<a href>`, not angle-bracket file autolinks for cross-note targets.
